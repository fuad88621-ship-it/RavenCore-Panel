# ⚠️ This file is GENERATED by install-panel.sh from your domains.
# Do not edit it here — run install-panel.sh and it will write the
# correct Caddyfile to /opt/raven/Caddyfile automatically.
#
# Example of what it generates:
#
# panel.example.com {
# 	reverse_proxy panel:3000
# }
#
# node.example.com is ONLY for the browser console WebSocket.
# The full agent API is never exposed publicly — the panel talks to the
# agent over the internal Docker network instead.
# node.example.com {
# 	@ws path_regexp ^/servers/[0-9a-f-]+/ws$
# 	reverse_proxy @ws agent:8080
# 	@notws not path_regexp ^/servers/[0-9a-f-]+/ws$
# 	respond @notws 404
# }
